This example is built by CMake when `COIN_BUILD_EXAMPLES=ON`.

Configure and build it with:

  cmake -S . -B build -DCOIN_BUILD_EXAMPLES=ON
  cmake --build build --target Switchboard SwitchboardOperator

The resulting shared libraries can be loaded by an Open Inventor
application that supports the dynamic extension example.
